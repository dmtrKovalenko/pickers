import Ad from 'components/Ad';
import Example from 'components/Example';
import PageMeta from 'components/PageMeta';
import * as DateFnsExample from 'examples/Date-fns.example'

<PageMeta title="Date-fns localization - @pickers/material-ui" />

# Localization with date-fns

When using date-fns, localization is performed by passing a locale object to the `LocalizationProvider` component.

<Ad />

## Basic example

_Note_: Any picker in the component tree will be re-rendered automatically on locale change.

<Example source={DateFnsExample} />
